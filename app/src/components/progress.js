import * as React from 'react';

const ProgressBar = () => {
    return <View style={styles.rectangle} />;
};
  
StyleSheet.create({
rectangle: {
    width: 100 * 2,
    height: 100,
    backgroundColor: "red",
},
});

export default ProgressBar;